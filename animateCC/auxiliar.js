var n;
var pos;

function activeCheck(n, pos){
    status[pos] = n;

    // if(window.API != null){
    //     suspendData = ScormProcessGetValue("cmi.suspend_data");
    //     if(ScormProcessGetValue("cmi.suspend_data").indexOf(slideChecked[Reveal.getState().indexh]) < 0){
    //         ScormProcessSetValue("cmi.suspend_data", suspendData + (slideChecked[Reveal.getState().indexh]));
    //     }
    // }    
}